cmake_minimum_required(VERSION 2.8.4)
project(test_add)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(test_add main.cpp)
target_link_libraries(test_add ${OpenCV_LIBS})
